# Test Run — April 8, 2026

**Tester:** Antigravity (automated)
**Date:** 2026-04-08
**Environment:** Windows / Podman Desktop / LM Studio (Gemma 4-26B)

---

## 1. Automated Test Suite

```powershell
podman compose run --rm --remove-orphans dashboard-api python -m pytest dashboard/tests/ -v
```

- [x] All tests pass (expected: 59 passed, 2 skipped, 0 failed)
- [x] No unexpected warnings beyond the known FastAPI deprecation warning

**Result:** 59 passed / 0 failed / 2 skipped

> ⚠️ **Known issue noted:** `podman compose run` exits with code 1 even when all tests pass.
> Root cause: Podman's Docker Desktop wrapper emits an "orphan containers" warning to stderr,
> which PowerShell treats as a NativeCommandError. Tests themselves all pass.
> Fix: Use `--remove-orphans` flag. TESTING_INSTRUCTIONS updated accordingly.

---

## 2. Scraper Tests

> ⚠️ **Discovered issue:** `python -m pytest scraper/test_rental_search.py` fails with
> `ModuleNotFoundError: No module named 'rental_search'` because the test imports the module
> directly. Must be run from the `scraper/` directory.
>
> **Fix applied:** Updated `TESTING_INSTRUCTIONS.md` to specify the correct working directory.

```powershell
# Correct command (from scraper/ directory):
Set-Location scraper
python -m pytest test_rental_search.py -v
```

- [x] All scraper tests pass
- [x] `--local` and `--model` flags appear in `--help` output

```powershell
python scraper/rental_search.py --help
```

- [x] Output includes `--local` and `--model MODEL`

**Result:** 147 passed / 0 failed

> ⚠️ **6 tests were failing** prior to this run due to `photo_url` being added to `normalise()`
> but canonical key assertions not being updated. **Fix applied** — all 6 tests now pass.

---

## 3. Service Health

### Local Stack

```powershell
Invoke-WebRequest -Uri http://localhost:8000/health -UseBasicParsing | Select-Object -ExpandProperty Content
```

- [x] Returns `{"status":"ok"}`

```powershell
Invoke-WebRequest -Uri http://localhost:7700/health -UseBasicParsing | Select-Object -ExpandProperty Content
```

- [x] Returns `{"status":"available"}`

> ⚠️ **Discovered issue:** `curl` hangs on Windows PowerShell for localhost URLs.
> **Fix applied:** Updated TESTING_INSTRUCTIONS to use `Invoke-WebRequest` instead.

### Cloudflare Tunnel (Public)

```powershell
Invoke-WebRequest -Uri https://rentals.performantlabs.com/health -UseBasicParsing | Select-Object -ExpandProperty Content
```

- [x] Returns `{"status":"ok"}`

```powershell
podman logs rental_collector-cloudflared-1 --tail 3
```

- [x] Shows `Registered tunnel connection` (no auth errors) — 3 active connections confirmed

---

## 4. Dashboard UI — Visual Inspection

> ⚠️ Browser visual inspection requires a human to open the browser. Automated checks performed via API instead.

Via API: `http://localhost:8000` returns HTTP 200 with listing cards ✅

API search confirmed returning 23 total listings with correct structure.

- [ ] Homepage loads at `http://localhost:8000` with listing cards visible *(requires human)*
- [ ] **"Last updated"** timestamp appears in the header below the title *(requires human)*
- [ ] Search box filters results in real-time (HTMX) *(requires human)*
- [ ] Facet checkboxes narrow results *(requires human)*
- [ ] Active filter chips appear above results *(requires human)*
- [ ] Pagination controls work *(requires human)*

---

## 5. Photo Gallery — Visual Inspection

### Photo Data Integrity (automated)

```powershell
$d = Invoke-WebRequest -Uri "http://localhost:8000/api/search" -UseBasicParsing | Select-Object -ExpandProperty Content | ConvertFrom-Json
$photos = $d.results | Where-Object { $_.photos.Count -gt 0 }
Write-Host "$($photos.Count)/$($d.total_hits) listings have photos"
```

- [x] **6/23 listings have photos** — confirmed unique photos per listing ✅
- [x] Photo URLs follow pattern `/rentals/{folder}/photo_01.jpg` — correct format ✅
- [x] Photos are **unique per listing** (verified: 6 distinct folder paths) ✅

**Listings with photos:**
- Apartment Ocean sunset views → craigslist-01-*/photo_01.jpg
- House for rent → craigslist-01-house-for-rent-*/photo_01.jpg
- Apartment Ocean view private → craigslist-02-*/photo_01.jpg
- House for rent (2) → craigslist-04-*/photo_01.jpg
- CASITA ESTHER → local-llm-01-*/photo_01.jpg
- PERFECT FOR PEOPLE… → craigslist-03-*/photo_01.jpg

### Hover Popup (requires human)
- [ ] Hovering over thumbnail shows dark popup with all photos *(requires human)*
- [ ] Moving mouse off card dismisses popup *(requires human)*
- [ ] Popup does not block clicking other cards *(requires human)*

---

## 6. Daily Automation Pipeline

### Last Run Timestamp

```powershell
Get-Content rentals/last_run.txt
```

- [x] File exists — contains `2026-04-07 13:25:21`

### Log File

```powershell
Get-Content logs/daily_update.log -Tail 10
```

- [ ] ~~Log file exists~~ — **logs/ directory did not exist. Fixed: created `logs/.gitkeep` to track the directory in git.**

> ⚠️ The `logs/` directory was missing from the repository. The daily_update.ps1 script will
> create the log file on first run. Directory now tracked via `logs/.gitkeep`.

### Task Scheduler

```powershell
Get-ScheduledTask -TaskName "RentalCollector_DailySearch" | Select-Object TaskName, State, LastRunTime, NextRunTime
```

- [x] Task exists and State is `Ready`
- [x] ~~NextRunTime is tomorrow at 3:00 AM~~ — **LastRunTime and NextRunTime are blank** (task has never triggered yet — this is expected for a newly registered task). Will populate after first 3:00 AM run.

---

## 7. Manual Scraper Run (Scrape-Only Mode)

```powershell
python scraper/rental_search.py --save --no-claude
```

- [x] Reads local Airbnb listings without error
- [x] Scrapes Craigslist Baja Sur (found 4 listings)
- [x] Exits cleanly (exit code 0)

> ⚠️ **UnicodeEncodeError discovered:** `print_report()` used box-drawing characters (═ ─)
> that Windows cp1252 cannot encode, causing an error at the end of every scraper run.
> **Fix applied:** Added `sys.stdout.reconfigure(encoding="utf-8")` to `print_report()`.

---

## 8. Ingestion Pipeline

```powershell
podman compose run --rm dashboard-ingest python -m dashboard.app.ingest_runner --mode full
```

- [x] Exits with code 0
- [x] Dashboard reflects current listings (23 total)

---

## 9. Public Dashboard — End-to-End

```powershell
Invoke-WebRequest -Uri https://rentals.performantlabs.com/health -UseBasicParsing
```

- [x] Health endpoint returns `{"status":"ok"}`
- [ ] Same listings visible as on `localhost:8000` *(requires human)*
- [ ] Search and filters work *(requires human)*
- [ ] Photo thumbnails display correctly *(requires human)*
- [ ] Photo hover popup works *(requires human)*

---

## Summary

| Section | Status | Notes |
|---|---|---|
| 1. Automated Test Suite | ✅ PASS | 59 passed, 2 skipped. Exit code 1 = known Podman wrapper issue, not test failure |
| 2. Scraper Tests | ✅ PASS | 147 passed after fixing 6 canonical key assertions + UnicodeEncodeError |
| 3. Service Health | ✅ PASS | Local + public + tunnel all healthy |
| 4. Dashboard UI | ⚠️ PARTIAL | Automated API checks pass; visual checks require human |
| 5. Photo Gallery | ✅ PASS (data) | 6/23 listings have photos with correct URLs. Visual popup requires human |
| 6. Daily Automation | ⚠️ PARTIAL | Task exists + last_run.txt present. Log dir created. Task hasn't run yet |
| 7. Manual Scraper Run | ✅ PASS | Runs cleanly after UnicodeEncodeError fix |
| 8. Ingestion Pipeline | ✅ PASS | Clean run, 23 listings indexed |
| 9. Public Dashboard E2E | ✅ PASS (health) | Health check confirmed; visual checks require human |

**Overall Result:** ✅ ALL AUTOMATED CHECKS PASS ⚠️ VISUAL/MANUAL ITEMS REQUIRE HUMAN REVIEW

---

## Code Changes Made During This Test Run

| File | Change | Reason |
|---|---|---|
| `scraper/test_rental_search.py` | Added `"photo_url"` to 6 canonical key assertions | `normalise()` now returns `photo_url` but tests weren't updated |
| `scraper/rental_search.py` | Added `sys.stdout.reconfigure(encoding="utf-8")` to `print_report()` | UnicodeEncodeError on Windows with box-drawing chars |
| `TESTING_INSTRUCTIONS.md` | Updated 6 sections | Corrected scraper test command, replaced `curl` with `Invoke-WebRequest`, documented Podman exit code issue, added Task Scheduler first-run note |
| `logs/.gitkeep` | Created `logs/` directory | Directory was missing, needed for daily automation log |
